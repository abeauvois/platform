{
  "name": "trading",
  "scripts": {
    "client": "cd client && bun run dev",
    "server": "bun --bun run --hot server/index.ts",
    "dev": "concurrently \"bun run server\" \"bun run client\"",
    "db:up": "docker compose up -d",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "db:seed": "bun run ./scripts/seed-local-db.script.ts",
    "open-auth-docs": "open-cli http://localhost:3001/api/auth/reference",
    "build": "bun build ./server/index.ts --outdir ./dist/server --target bun && cd client && bun run build",
    "start": "bun run ./dist/server/index.js",
    "docker:build": "docker build -t hono-todo-app .",
    "docker:run": "docker run -p 3001:3001 --env-file .env -e DATABASE_URL='postgresql://user:password@host.docker.internal:5432/trading_db' hono-todo-app"
  },
  "dependencies": {
    "@hono/zod-openapi": "^1.1.5",
    "@hono/zod-validator": "^0.7.2",
    "@neondatabase/serverless": "^1.0.1",
    "@platform/auth": "workspace:*",
    "@platform/env": "workspace:*",
    "@platform/db": "workspace:*",
    "@platform/trading-domain": "workspace:*",
    "@scalar/hono-api-reference": "^0.9.25",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.8.12",
    "pg": "^8.16.3",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.15.5",
    "@types/ws": "^8.18.1",
    "concurrently": "^9.2.0",
    "drizzle-kit": "^0.31.4",
    "drizzle-seed": "^0.3.1",
    "open-cli": "^8.0.0"
  }
}