[build]
buildCommand = "cd apps/trading-server && bun run build"

[deploy]
startCommand = "cd apps/trading-server && bun run start"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
