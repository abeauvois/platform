[build]
buildCommand = "bun run build"

[deploy]
startCommand = "cd apps/trading-server && bun run start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
