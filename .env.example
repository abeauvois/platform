# =============================================================================
# Platform Environment Configuration
# =============================================================================
# Copy this file to .env and fill in the required values.
# Variables marked [REQUIRED] must be set; others have defaults.
#
# Both API Server and Trading Server validate their environment variables
# at startup and will fail fast with clear error messages if required
# variables are missing.
# =============================================================================

# -----------------------------------------------------------------------------
# Database [REQUIRED]
# -----------------------------------------------------------------------------
# PostgreSQL connection string - required by both API and Trading servers
DATABASE_URL=postgresql://platform:platform@localhost:5432/platform

# -----------------------------------------------------------------------------
# Application Environment
# -----------------------------------------------------------------------------
# Options: development, production (default: development)
APP_ENV=development

# -----------------------------------------------------------------------------
# Port Configuration
# -----------------------------------------------------------------------------
# For worktrees/parallel development, set PORT_OFFSET (e.g., 100, 200)
# Ports will be calculated as: base_port + PORT_OFFSET
PORT_OFFSET=0

# API Server port (default: 3000)
API_PORT=3000

# Dashboard client port (default: 5000)
DASHBOARD_PORT=5000

# Trading server port (default: 3001)
TRADING_SERVER_PORT=3001

# Trading client port (default: 5001)
TRADING_CLIENT_PORT=5001

# -----------------------------------------------------------------------------
# URL Configuration
# -----------------------------------------------------------------------------
# These are auto-derived from ports if not set, but can be overridden
# for production deployments with custom domains

# API Server URL (default: http://localhost:3000)
API_URL=http://localhost:3000

# Dashboard URL (default: http://localhost:5000)
DASHBOARD_URL=http://localhost:5000
CLIENT_URL=http://localhost:5000

# Trading Server URL (default: http://localhost:3001)
TRADING_SERVER_URL=http://localhost:3001

# Trading Client URL (default: http://localhost:5001)
TRADING_CLIENT_URL=http://localhost:5001

# Platform API URL for CLI and clients
PLATFORM_API_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Authentication [REQUIRED]
# -----------------------------------------------------------------------------
# IMPORTANT: This secret MUST be the same across all servers that share
# authentication sessions. Different values will cause 401 errors.
BETTER_AUTH_SECRET=your-secret-here

# Base URL for better-auth (should point to API server)
BETTER_AUTH_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
# Additional allowed origins for CORS (comma-separated)
# Leave empty if only using the default client URLs above
CLIENT_URLS=

# -----------------------------------------------------------------------------
# Trading Server - Binance API (Optional)
# -----------------------------------------------------------------------------
# These are optional - if not set, authenticated trading routes (balances,
# orders) will be disabled, but public market data routes will still work.
BINANCE_API_KEY=
BINANCE_API_SECRET=

# -----------------------------------------------------------------------------
# API Server - Optional Service Integrations
# -----------------------------------------------------------------------------
# These are optional - features requiring these keys will be disabled if not set

# Anthropic API for AI features
ANTHROPIC_API_KEY=

# Notion integration
NOTION_INTEGRATION_TOKEN=
NOTION_DATABASE_ID=

# Twitter/X API
TWITTER_BEARER_TOKEN=

# Gmail OAuth integration
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=
MY_EMAIL_ADDRESS=
