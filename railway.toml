[build]
buildCommand = "bun run build"

[deploy]
# startCommand removed - each service uses its Dockerfile CMD/ENTRYPOINT
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
