# Railway configuration for monorepo
# Configure individual services via Railway dashboard or environment variables

[build]
builder = "dockerfile"

# Service-specific settings should be configured in Railway dashboard:
# - API: dockerfilePath = "apps/api/Dockerfile"
# - Trading: dockerfilePath = "apps/trading/Dockerfile"
#
# Set these in Railway service settings:
# 1. Root Directory: (leave empty for repo root)
# 2. Dockerfile Path: apps/api/Dockerfile (or apps/trading/Dockerfile)
